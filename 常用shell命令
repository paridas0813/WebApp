man -命令 查看命令帮助
清屏 ctrol+l
pwd 列出当前工作目录
vi A进入编辑模式 退出 先按esc 再按:q!
chmod 修改权限 
grep 利用正则表达式进行全局收索并打印所有包含它的行
ps 列出当前出于执行状态的进程 -f列出更为详细的内容 kill -进程pid
top显示所有进程信息
ctrol+c结束当前打印
ping 网站 用于测试是否可以与远程进行通信
telnet 网站 请求连接登陆远程
finger 指令用于显示指定主机上有关用户的信息。这里的主机可以是本地主机，也可以是远程服务器。
